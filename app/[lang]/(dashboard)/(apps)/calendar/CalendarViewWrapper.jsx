'use client';

import React from 'react';
import CalendarView from './calender-view';

const CalendarViewWrapper = (props) => {
  return <CalendarView {...props} />;
};

export default CalendarViewWrapper;
